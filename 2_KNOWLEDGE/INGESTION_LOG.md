# Ingestion log

Audit trail of every external repo/knowledge ingested into SEOSONA Video. Process:
`6_SOP/REPO_VETTING_SOP.md`. One line per ingestion: date · source · license · what · where · why.

| Date | Source | License | What was taken | Where | Why |
|------|--------|---------|----------------|-------|-----|
| 2026-07-06 | langgenius/dify | Apache-2.0 **+ 2 conditions** (no multi-tenant resale, no logo removal — both frontend-only; headless single-box use unaffected) | **HARVESTED (clean-room)** — DAG node-taxonomy → tiny Python topo-sort runner | `4_BRAIN/pipeline_dag.py` (Node/Pipeline + Kahn topo-sort + to_graph; canonical stages discover→script→render→quality→publish; executors DELEGATE to topic_to_video/workflow_router, zero logic dup) | 147k★ visual LLM-app platform (Docker: Flask+Next+PG+Redis+Celery+vectorDB) — platform itself REFERENCE (duplicates OODA loop+cascade+library). Took only the SHAPE of `api/core/workflow/` (typed node registry + topology sort): stages are now DATA, rearrangeable in one place. No Dify code, no Docker. VERIFIED: `python 4_BRAIN/pipeline_dag.py list/graph/run` |
| 2026-07-06 | simstudioai/sim | Apache-2.0 (verified unmodified) | **HARVESTED (clean-room)** — node-graph canvas UX → dashboard Pipeline view | `9_DASHBOARD` Pipeline view (`/api/pipeline` → `pipeline_dag.to_graph()`; vis-network hierarchical L→R DAG, click→stage inspector) | 29k★ Figma-style agent-workflow canvas (TS/Next.js+Bun+PG/pgvector SaaS) — platform REFERENCE (redundant). Took only the interaction pattern (visual node-graph of the pipeline), realized in our existing vis-network — NO ReactFlow, no new dep. VERIFIED in-browser: 5 stages L→R, click opens inspector, no console errors |
| 2026-07-06 | langchain-ai/open-agent-platform | MIT | **REFERENCE** (≈ignore — deprecated upstream) | REPO_WATCHLIST | No-code web UI (100% TS/Next.js, 0 .py) = thin client REQUIRING LangGraph Platform + LangConnect + Supabase = exactly the framework lock-in SEOSONA's hand-rolled resilient llm_engine rejects. Fully redundant + deprecated (README → LangSmith Agent Builder). Only idea worth remembering (concept-only) = the HITL "agent-inbox" approval pattern IF a human review gate is ever added to the OODA loop |
| 2026-07-06 | Mintplex-Labs/anything-llm | MIT | **HARVESTED (clean-room)** — chunk-provenance prepend | `4_BRAIN/knowledge_graph.py` new `recall_context()`+`_cite()` (each recalled chunk prefixed `[src: source - title - kind/verdict - date]`) + `recall(...)` now returns `date` + `gen_knowledge_notes.py` captures the INGESTION_LOG date + CLI `recall … --cite` | 62k★ all-Node doc-chat RAG app — platform REFERENCE (duplicates cascade+KG, zero Python). Took only the `server/utils/TextSplitter/index.js` idea (stamp each chunk with where it came from BEFORE the text) so an LLM grounding on recalled knowledge can cite {source,title,date} — reinforces the no-fabricated-numbers traceability gate. VERIFIED: cited recall block renders with dated provenance headers |
| 2026-07-06 | HKUDS/AutoAgent | MIT | **REFERENCE** (redundant, no harvest) | REPO_WATCHLIST | 9.4k★ zero-code NL→agent FRAMEWORK (Python but a Docker-sandboxed agent-CREATION platform, litellm, browser_env). Every capability exists lighter+purpose-built: orchestration=OODA loop+orchestrator persona; multi-provider+fallback=llm_engine cascade (MORE resilient — parse-or-continue tiering litellm lacks); research=researcher.py+deep-research; memory=knowledge graph; self-improvement=eval flywheel. Its unique angle (generate NEW agents from NL) solves a problem a fixed brand-locked video line doesn't have. Zero video/TTS/render overlap. No adopt, no harvest |
| 2026-07-06 | f/prompts.chat | code MIT / **prompts CC0** | **REFERENCE only** (CC0 = eligible, no gap) | REPO_WATCHLIST | "Awesome ChatGPT Prompts" — huge community collection of generic single-shot "act as X" chat prompts (164k★) + a self-host Next.js prompt platform. NOT tuned for a VN video-factory pipeline; SEOSONA already has 17 tuned domain SKILL.md (SEO/marketing/content/copywriting/CRO/design) + `9_PROMPTS` + per-stage LLM system-prompts — deeper + on-brand. Platform = overkill. CC0 so a handful of role prompts (SEO/storyteller/copywriter) could seed a NEW capability's prompt later, but no current gap → adopt nothing |
| 2026-07-06 | mvanhorn/last30days-skill | MIT | **HARVEST (clean-room)** — keyless recency+engagement topic discovery: HackerNews points + GitHub-stars(pushed>date) + arXiv, 30-day window, round-robin fused/deduped/on-domain | `4_BRAIN/researcher.py` new `recency_topics()` + `_hn_recent/_github_recent/_arxiv_recent`; wired into `demand_topics(recency=True, days=30)` | the FRESHNESS axis a tech-NEWS factory lives on (trends_vn/expand_keywords were recency-blind); fixes the known "News RSS drifts off-subject" risk with engagement + domain gate. VERIFIED. Only keyless tech subset; social/paid (X/TikTok/IG/Polymarket/Perplexity) SKIPPED as off-mission |
| 2026-07-06 | RayVentura/ShortGPT | MIT | **REFERENCE only** | REPO_WATCHLIST | older/weaker sibling of Pixelle-Video/Auto-Create-Video; MoviePy stock-footage-over-VO + burned captions — SEOSONA surpasses on render(HTML+GSAP)/captions(ASS karaoke)/ASR(PhoWhisper)/TTS(OmniVoice)/LLM(cascade). Footnote only: `getSpeechBlocks` 0.8s-silence sentence split; dub-to-slot atempo already in `dub_align.py` |
| 2026-07-06 | nagisanzenin/engram | MIT | **REFERENCE only** (out of domain) | REPO_WATCHLIST | NOT agent-memory — a human-learning FSRS spaced-repetition tutor for Claude Code (stdlib, no MCP/DB/embeddings); zero overlap with knowledge_graph.py. Harvestable IDEA (note only): "blind assessor + append-only receipts / separation-of-powers" for the eval flywheel |
| 2026-07-06 | apify/fingerprint-suite | Apache-2.0 | **REFERENCE only** (don't adopt) | REPO_WATCHLIST | browser-fingerprint gen/inject for Playwright anti-bot evasion. researcher.py = urllib (no browser); capture_scroll.js targets CHOSEN pages not hostile sites → inapplicable + overkill + ToS/ethics liability. If ever blocked: real browser channel + honest UA + swap target, NOT evasion tooling |
| 2026-07-06 | weaiw/trove-ai | **AGPL-3.0** | **REFERENCE only** (clean-room idea) | REPO_WATCHLIST | AGPL personal-knowledge-base web-app (Next.js+FastAPI+pgvector RAG); architectural twin of knowledge_graph + script_writer, no engine gap. AGPL = never vendor. One clean-room idea: explicit "insufficient-evidence → flag" critique gate for `script_writer.py` verify stage |
| 2026-07-06 | internal (native 9:16 mockups) | own | 3 native MOCKUP components — `notification` (phone/OS notification card), `social` (social-post card, no fake followers), `phone` (phone device + app screen); light-brand, real scene data, `.ritem`, seek-safe | `4_BRAIN/native_composer.py` (+CSS) · `frame_study.py` · `component_picker.py` · `spec_lint.py` | The achievable, render-verified answer to "97 block thành kho mockup dùng được" — fills the genuine gaps the demos pointed to (code/chart/terminal already native). Catalog 48 frames, 0 drift. Block-forge kept for future strong-LLM batch. |
| 2026-07-06 | internal (block forge → 9:16 kho) | own | `4_BRAIN/block_forge.py` — turns a 16:9 gallery block into a 9:16 light injectable template: dim-swap 1920×1080→1080×1920 + strip chrome + LLM re-layout to light+mustache-slots + render-validate + fake-data QC → `7_ASSETS/blocks_9x16/` kho; `render_filled(name,data,out)` bombs real scene data in at video time | `4_BRAIN/block_forge.py` | User: 97 blocks must be 9:16 + auto-injected with real content = a usable mockup kho. Mechanism BUILT + proven: 9:16 render verified (1080×1920), QC refuses fake-data blocks, LLM re-layout clean on small mockup blocks (macos-notification). Honest yield: small html/css mockups convert; 40k-code/shaders don't (LLM output limit) + needs cloud LLM (was 429). Batch: `python 4_BRAIN/block_forge.py --all` when quota's up; kho reset empty pending that. |
| 2026-07-06 | internal (image relevance + block vetting) | own | (A) `image_sourcer._build_query` VN→EN lexicon + relevance gate — a scene gets a CORRECT photo or NONE, never a wrong one; (B) `plan_scenes` ≥60% visual-coverage governor + log; (C) `hf_blocks` demo-strip + fake-data QC (`render_block` REFUSES leaks) + `scripts/vet_blocks.py` → `BLOCK_VET_MANIFEST.json` | `2_SKILLS/image_sourcer/image_sourcer.py` · `4_BRAIN/video_engine.py` · `4_BRAIN/hf_blocks.py` · `scripts/vet_blocks.py` | User: images must be accurate, ≥60% scenes visual, and the 97 blocks usable with REAL data. A+B done+verified. C: safety gate built + all 97 vetted (27 fx / 37 redundant / 33 fake) + RENDER-verified → blocks are DARK 16:9 gallery demos (not light 9:16 assets); static QC misses JS-generated fakes ($5,169). Verdict: blocks stay OFF; native richness (45 comps + relevance-gated images + effect_library + frame-synth) is the real path. |
| 2026-07-06 | internal (self-sufficient richness) | own | **Factory enriches itself without an external LLM** — `component_picker.fill()` GUARANTEES a real non-fabricated visual per bare middle scene (cue `pick()` → rotating `photocard/quote/callout/tip`, varied, honest data only); wired into `video_engine.plan_scenes` as the richness floor; photocard fallback activates the image kho (Pexels auto-source at render) | `4_BRAIN/component_picker.py` (`fill`, `_FILL_ROTATION`) + `video_engine.plan_scenes` | Test news video was SPARSE because visual richness leaned on a LIVE LLM (`enrich_llm`) that was rate-limited to death. Proven fix: re-ran with `SEOSONA_LLM_COMPONENTS=0` → 7 scenes, 6 distinct visuals, 0 bare-text, photocard sourced a real photo, PASS 100/100. Block kho stays OFF (registry blocks = fake-data demos → would violate no-fakes). |
| 2026-07-06 | internal (frame-study self-grow) | own | **Self-growing frame kho** — `4_BRAIN/frame_synth.py`: when a scene fits no built-in frame, the art-director LLM proposes a NEW frame (light-brand, seek-safe mustache HTML, no `<script>`) → `validate()` (no dark bg / no JS / no external URL / renders / balanced) → `register()` into `7_ASSETS/frames/generated_frames.json` → reused forever | `frame_synth.py` + wired: `native_composer._component` fallback (`render_generated`) + `_reveal_count`; `frame_study._effective()` merges generated frames into menu/kinds/by_group/selfcheck; `component_picker._allowed()`/`_validate` accept them + `enrich_llm` `"kind":"__new__"` triggers `grow()` | Answers "hyperframe tự sinh thêm nếu kho chưa có, gom vào kho làm giàu kho" — the 45-frame kho is no longer fixed; it GROWS from real content need. Verified end-to-end: hand-authored `faq_flip` spec → registered → rendered through full HyperFrames pipeline (2 FAQ cards, light-brand, `.ritem` reveal) → kho reset clean (grows only from real runs). |
| 2026-07-03 | TMElyralab/MuseTalk | MIT (code) | ADOPTED as Engine #3b — mouth-inpaint lip-sync; **DWPose(mmpose+mmcv) swapped → MediaPipe FaceMesh** to kill the Windows mmcv blocker | `2_KNOWLEDGE/external_toolkits/MuseTalk/` (patched) + `.venv-musetalk` + `scripts/lipsync_musetalk.py` + `6_SOP/LIPSYNC_ENGINE_MUSETALK.md` | real face/video + CQA voice → lip-sync; verified. Weights ~4GB (unet/sd-vae/whisper/face-parse) via `scripts/download_musetalk_weights.py` |
| 2026-07-03 | OpenMOSS/MOVA | Apache-2.0 | **REFERENCE only — NOT adopted** | REPO_WATCHLIST | Joint audio-visual *diffusion* (8s clips, ≥24GB GPU, Py3.12, flash-attn/NCCL → hostile to Windows/3060); wrong shape for long-form VN text-grounded news. Re-check only if a ≥24GB GPU + generative b-roll are both wanted. |
| 2026-07-03 | jianchang512/pyvideotrans | **GPL-3.0** | **REFERENCE / CLEAN-ROOM** — algorithm only, NO code copied (GPL would infect) | studied `videotrans/task/_rate.py`, `docs/Synchronize.md` → own impl `4_BRAIN/dub_align.py` | The "fit dubbed audio to video timeline" algorithm (timeline-extension → speed-audio / slow-video / split-deficit) — the missing organ for Engine #4 Localize (foreign video → VN dub in-sync). Skipped its GUI/ASR/TTS (we have better). |
| 2026-07-03 | DanielSWolf/rhubarb-lip-sync | MIT (output ours) | RE-ASSESSED (already Engine #4) → built **lip-sync QC** using its viseme timeline | `scripts/lipsync_qc.py` (Rhubarb × MediaPipe desync detector) | untapped value: independent QC for the new MuseTalk engine (matched corr ~0.25-0.33 vs mismatched ~ −0.25); phonetic recognizer already VN-correct |
| 2026-06-30 | AIDC-AI/Pixelle-Video | Apache-2.0 | REFERENCE only — NOT adopted | `2_KNOWLEDGE/external_toolkits/Pixelle-Video/` (gitignored) | Complete competing auto-video engine (topic→AI-image/video-per-sentence via ComfyUI/Wan/Seedream/Kling→TTS→BGM). SEOSONA already has a full brand-specific pipeline; AI-illustration imagery conflicts with the clean tech-editorial brand → don't recreate/over-build. Keep as reference for the API-media-provider abstraction IF AI b-roll is ever wanted. NOTE: REPO_INVENTORY tags it "S-Core" — downgrade to REFERENCE. (Clone had landed outside the repo via a relative-path UAP clone from wrong CWD; moved back in.) |
| 2026-06-29 | boraoztunc/skills | Apache-2.0 (ogilvy: MIT) | 13 domain SKILL.md (SEO, copywriting, content, CRO, design/UX) | `2_KNOWLEDGE/domain_skills/` | ground video content in real SEO/marketing/design expertise |
| 2026-06-29 | nextlevelbuilder/ui-ux-pro-max-skill | MIT | 4 design SKILL.md (design-system, brand, ui-styling, banner-design) | `2_KNOWLEDGE/domain_skills/` | deeper on-screen/design + brand + thumbnail craft (non-dup with boraoztunc) |
| 2026-06-29 | VinAIResearch/PhoWhisper (vinai/PhoWhisper-medium) | BSD-3 | CT2 int8 convert → bundled model | 7_ASSETS/models/phowhisper-medium-ct2 | best VN ASR (−40% WER vs Whisper); medium = ~2x faster/½ size of large, ~same WER |
| 2026-06-29 | D:\SEOSONA AIideo talking heads (4 reels) | reference | style references (caption/card/transition craft) | 2_KNOWLEDGE/style_references/talking_head/ | learn talking-head editing craft + new template ideas (SKILL.md was dup → skipped) |
| 2026-06-29 | SEOSONA proposals (2 PDF, internal) | internal/own | extracted text (markitdown) → brand facts | 3_MEMORY/brand_memory/proposals_extracted/ + filled .claude/product-marketing-context.md | learn the real SEOSONA brand (services/C.B.O/proof/CTA) |
| 2026-06-29 | coderamp-labs/gitingest | MIT | pip dependency (CLI repo→text digest) | requirements.txt | SYSTEM: turn any repo into one text blob for fast analysis/vetting |
| 2026-06-29 | facebook/astryx | MIT | REFERENCE only (design-system/token patterns) | REPO_WATCHLIST.md | FACTORY: SEOSONA has DESIGN.md+craft; reference astryx for token/system ideas, don't vendor the React lib |
| 2026-06-29 | microsoft/markitdown | MIT | pip dependency + wrapper | `2_KNOWLEDGE/scripts/ingest_source.py` + requirements.txt | SYSTEM: turn any PDF/DOCX/URL → clean markdown to learn from (ingestion capability) |
| 2026-06-27 | nexu-io/open-design | Apache-2.0 | HyperFrames craft (motion-principles, css-patterns, palettes, transitions) | `2_KNOWLEDGE/hyperframes/craft/` | richer, on-brand motion for the 14 components |
| 2026-06-27 | digitalsamba/claude-code-video-toolkit | MIT | WPM-pacing insight + ffmpeg/brand refs | `2_KNOWLEDGE/external_toolkits/claude-code-video-toolkit/` | fix voice pacing variance (ported into native_composer) |

| 2026-06-29 | swivid/f5-tts | MIT code / **CC-BY-NC weights** | F5 BACKUP voice path (gated, subprocess-isolated) | (planned `f5_backup.py` — NOT built/removed; voice_router is the live path) | redundant clone-voice backup; DORMANT (base non-commercial; needs commercial VN ckpt) |
| 2026-06-29 | yt-dlp/yt-dlp | Unlicense | (planned) footage-sourcing util — pin as tool | (backlog) `scripts/source_footage.py` | robust download for course footage |

| 2026-06-29 | huytranvan2010/AI-auto-generate-video | (analyzed) | sync principle: audio=master clock, captions from real audio timings (not estimated) | applied →  ASR-anchored karaoke | fix karaoke-voice drift |

| 2026-06-29 | hoquanghai/Auto-Create-Video | MIT | (analyzed) confirms scene-level audio-master sync (HyperFrames, VN); port idea: 8ms boundary micro-fade + SFX-from-measured-starts | (backlog) | sync validation + anti-click |

| 2026-06-29 | k2-fsa/OmniVoice | Apache-2.0 | (analyzed) VN TTS + clone YES but NO word-timestamps + torch2.8 conflict → not sync-friendly | REPO_WATCHLIST | future VN A/B only |

| 2026-06-29 | (lineage note) | — | AI-auto-generate-video = FORK of hoquanghai/Auto-Create-Video; both build on nexu-io/html-video (HTML→video) + use k2-fsa/OmniVoice (local VN TTS server :8123, NO SRT) | — | understand the repo family |
| 2026-06-30 | huytranvan2010/AI-auto-generate-video + loha-video-maker SKILL | MIT (repo) | CRAFT adopted (re-skinned to brand, light-only): liquid brand-colour depth blobs (gsap), fitText auto-scale, semantic SFX override, REAL screenshot in mockup/repo scene (Playwright via system Edge), multi-screenshot (2 imgs/video), verify-gate wiring + ready-to-post sidecars (voice.mp3/script.txt/caption.txt) | `4_BRAIN/native_composer.py` · `make_video.py` · `scripts/capture_shot.js` | FACTORY: design-led visuals + post-ready output. Their dark-neon palette + OmniVoice NOT taken |
| 2026-06-30 | google/agents-cli | Apache-2.0 | (analyzed) PATTERN only — "Quality Flywheel" → built native `eval_judge.py` (Gemini-vision judge, 3-tier fallback) + `eval_run.py` + `1_CONFIG/eval_datasets/`. Skill-authoring patterns noted | `4_BRAIN/eval_judge.py` · `eval_run.py` · `6_SOP/EVAL_FLYWHEEL.md` | SYSTEM/QA: qualitative judge metadata can't do. Google-Cloud/Vertex/BigQuery/GEPA REJECTED (paid/cloud, conflict free-local) |
| 2026-06-30 | SEOSONA OS (`~/.seosona/4_AGENTS/personas`) | internal | PORTED + adapted 3 analysis agents to fill the analyze/learn gap (Video only had factory-strategist + video-discovery): `researcher`, `scout`, `security-auditor` → re-scoped to video + wired to Video paths (not OS refs). Built `.agents/skills/capability-analyst` (runs SELF_IMPROVEMENT_LOOP). | `1_AGENTS/personas/{researcher,scout,security-auditor}.md` · `.agents/skills/capability-analyst/SKILL.md` | SYSTEM: dedicated analyze/learn agents+skill. SKIPPED (dup/over-engineer): content-strategist/creator (have domain_skills), analytics (have ledger), intent_router/ingest_skill/AAAK/YouTube-connectors. Bulk repo analysis DELEGATES to OS UAP |

| 2026-06-30 | greensock/gsap-skills | MIT | (PART-B batch1) REFERENCE — GSAP agent-instruction set, ~90% dup with our `hyperframes/craft/motion-recipes-seosona.md` (same upstream). Kept cloned for pattern lookup. Verified the immediateRender first-frame-flash risk is N/A here (scene-0 uses tl.set, later scenes opacity-gated; frame-0 correct). | (reference only) | no rebuild — dedup |
| 2026-06-30 | feicaiclub/video-spec-builder | MIT | (PART-B batch1) REFERENCE — interactive Claude/Cursor spec skill (5-phase Q&A) ≠ our autonomous factory. Idea-backlog (not built, no-bloat): finer component subtypes, per-scene validate checklist (component ≤4×), 9:16 layout rules, audio-timeline. | (reference only) | no rebuild — different paradigm |

| 2026-06-30 | every-app/open-seo | MIT | (PART-B batch2) PATTERN adopted — its keyword/rank/SERP tools all need PAID DataForSEO → SKIPPED (free/local rule). Built native free/local `.agents/skills/video-seo-metadata` (topic→title/desc/tags/hashtags) from its workflow shape. Fills a real gap (no video-SEO metadata before). | `.agents/skills/video-seo-metadata/SKILL.md` | SEO: publish-ready metadata, no paid API |
| 2026-06-30 | shanraisshan/claude-code-best-practice | MIT | (PART-B batch2) PATTERN adopted — REFERENCE (SIL more mature). Took "Execution Contract + Fail-Closed Guardrail" + when_to_use routing → hardened the 3 personas + capability-analyst. Skipped over-reach (new SOPs/templates/hooks). | `1_AGENTS/personas/{researcher,scout,security-auditor}.md` · `.agents/skills/capability-analyst/SKILL.md` | SYSTEM: agents no longer improvise/silent-fail |

| 2026-06-30 | (DEEP RE-DIG of batch 1&2 — user: "repos hide a lot") | — | Re-analyzed the 4 REFERENCE repos with the new SIL stage-2b DEEP-EXTRACTION. Real gems harvested: video-spec-builder → scene-tag vocab + pacing archetypes + validation rules + tag→component → `2_KNOWLEDGE/VIDEO_CRAFT_RULES.md`. gsap-skills → easing menu + CustomEase presets + stagger choreography + gsap.utils data-motion → same doc. open-seo → keyword-intent taxonomy + discovery workflow + title/desc/tags formulas → enriched `video-seo-metadata` skill. cc-best-practice → frontmatter fields (when_to_use/paths/model/context:fork) + safety-hook ideas = BACKLOG (harness-config, no-bloat). | `2_KNOWLEDGE/VIDEO_CRAFT_RULES.md` · `.agents/skills/video-seo-metadata/SKILL.md` | confirms: dig deeper finds 5–15% hidden gold even in "REFERENCE" repos |

| 2026-06-30 | multica-ai/andrej-karpathy-skills | **MIT** (declared in SKILL.md — gh-api wrongly said NO-LICENSE; user caught the skip) | (PART-B batch3) ADOPTED — Karpathy's 4 LLM-coding principles (think-before-coding, simplicity-first, surgical-changes, goal-driven) → `6_SOP/LLM_CODING_DISCIPLINE.md`. Fixed the rule: "no LICENSE file" ≠ no license (check manifest); no-license blocks vendor, NOT analysis — never skip without looking. | `6_SOP/LLM_CODING_DISCIPLINE.md` | SYSTEM: agent discipline, reinforces no-bloat/surgical/verify |
| 2026-06-30 | nidhinjs/prompt-master | MIT | (PART-B batch3, 5/5 files) ADOPTED — hardened `_gemini_script` with explicit role + GROUNDING (no fabricated stats/features) + brand-tone lock (Gemini hallucinates/drifts). Other gems (template library, prompt SOPs) = backlog. | `4_BRAIN/make_video.py` (_gemini_script) | CONTENT: cleaner, grounded scripts |
| 2026-06-30 | nicobailon/visual-explainer | MIT | (PART-B batch3, 100% skill) ADOPTED knowledge → VIDEO_CRAFT_RULES §6: depth tiers, min-width:0 overflow guard, scene-tag label (static CSS = seek-safe). REJECTED its `--i` CSS-@keyframes stagger (agent mislabelled seek-safe — wall-clock, desyncs our GSAP-seek). Mermaid/slide-engine = backlog. | `2_KNOWLEDGE/VIDEO_CRAFT_RULES.md` | FACTORY: card hierarchy + overflow safety |
| 2026-06-30 | EverMind-AI/EverOS | Apache-2.0 (confirmed) | (PART-B batch4, 42/614 files, core memory paths) ADOPT-as-knowledge: its **Reflect & Consolidate** memory pattern → new SIL stage 10. **observability → BUILT-light batch10** (`4_BRAIN/factory_metrics.py` flight-recorder: 1 JSONL row/video, pass-rate/duration/size; `npm run factory:metrics`). Cascade-daemon / prompt-slots = still BACKLOG (heavy). #4 3-tier-fallback = SKIP-DUP (we have it). | `6_SOP/SELF_IMPROVEMENT_LOOP.md` (stage 10 + backlog note) | SYSTEM: self-improvement loop |
| 2026-06-30 | tuanminhhole/openclaw-skill-learning-memory | MIT (confirmed) | (PART-B batch4, 8/8 files = 100%) SKIP-DUP — a thinner version of our SIL (no vetting/security/dedup/audit). 1 gem extracted: **session-close reflection** → folded into SIL stage 10a. No code vendored. | `6_SOP/SELF_IMPROVEMENT_LOOP.md` (stage 10a) | SYSTEM: learn-loop habit |
| 2026-06-30 | datalab-to/lift | Apache-2.0 code / **OpenRAIL-M model (commercial-compete restricted)** | (PART-B batch4, 18/18 modules = 100%) SKIP-DUP — markitdown+gitingest already cover doc/repo ingestion. Unique = schema-driven PDF→JSON, but no current use-case + model-license risk for a commercial factory. BACKLOG: revisit only if data-viz content needs structured PDF extraction (HF backend, isolated skill). | (none — not adopted) | flagged license risk |
| 2026-06-30 | ArcReel/ArcReel | **AGPL-3.0** (analyze-only; NOT vendored — copyleft) | (PART-B batch5, ~70% targeted) REFERENCE-pattern. Most patterns (reference-image consistency, character design-sheets, cross-shot image anchoring, dialogue) = N/A to our faceless HTML engine → SKIP. The ONE transferable gem (CONVERGES with Toonflow): **two-stage script — plan the arc, then write scenes**. Re-implemented natively. | `4_BRAIN/make_video.py` (`_gemini_outline`) | CONTENT: coherent, non-repetitive scenes |
| 2026-06-30 | HBAI-Ltd/Toonflow-app | Apache-2.0 | (PART-B batch5, targeted brain: scriptAgent+productionAgent, skipped 282MB Electron/assets) ADOPT-pattern: **two-stage decomposition** (director-plan → storyboard) → built native `_gemini_outline` (Stage 1 arc) feeding the existing scene writer (Stage 2). Emotion-0-10 / continuity-locks / mode-routing / multi-char dialogue = N/A or BACKLOG (our engine ≠ image-gen drama). | `4_BRAIN/make_video.py` (two-stage `_gemini_script`) | CONTENT: arc-driven script |
| 2026-06-30 | DietrichGebert/ponytail | MIT (67k★, 100% core) | (PART-B batch6/skills) REFERENCE — its "lazy/reuse-ladder" philosophy = what we already adopted via [[Karpathy LLM_CODING_DISCIPLINE]]. 1 micro-gem: **tagged-shortcut comments** (`# TODO(seosona):`) → LLM_CODING_DISCIPLINE §3. Its review/debt skills = DUP (capability-analyst + UPGRADE_BACKLOG) → not built. | `6_SOP/LLM_CODING_DISCIPLINE.md` | SYSTEM: debt not silent |
| 2026-06-30 | yaojingang/yao-meta-skill | MIT (18 files core) | (PART-B batch6/skills) REFERENCE — a full enterprise "skill-OS" (maturity tiers, governance scoring, blind A/B, Skill-IR, telemetry) = over-built for our ~20-skill solo factory. 1 lightweight gem: **skill-qualification gate** ("is this even a skill?") → capability-analyst BUILD pre-check. Heavy machinery revisit only at 100+ skills. | `.agents/skills/capability-analyst/SKILL.md` | SYSTEM: anti skill-bloat |
| 2026-06-30 | github/spec-kit | MIT (116k★, templates+method) | (PART-B batch6/skills) REFERENCE — spec→plan→tasks + constitution + test-first is a heavyweight SDD methodology; its disciplines are ALREADY covered by our SIL + LLM_CODING_DISCIPLINE (think→simplest→surgical→verify). Building its 3 template SOPs = bloat for a solo factory → declined. | (none — REFERENCE) | discipline already covered |
| 2026-06-30 | **calesthio/OpenMontage** | **AGPL-3.0** (analyze-only, NOT vendored) | (PART-B batch7, user-flagged "trending" — FAN-OUT 3-agent deep dig: architecture+render+prompts, ~150 files, coverage stated per slice) **ADOPT-pattern**: enriched `_gemini_outline` with **hook patterns + narrative-structure variety + climax marking** (kills "video giống nhau") + VIDEO_CRAFT_RULES §7. **REFERENCE/BACKLOG** (over-build for solo 1-pipeline): declarative pipeline-manifests, stage-director skills, tool-registry/selector, checkpoint-resume, executable-playbook YAML, 5-aspect grammar, CHAI oversight, hero_moment render-flag. Image/character/Remotion/avatar = N/A (faceless HTML). Security clean. | `4_BRAIN/make_video.py` (_gemini_outline), `2_KNOWLEDGE/VIDEO_CRAFT_RULES.md` §7 | CONTENT: varied non-formulaic arcs |
| 2026-06-30 | headroomlabs-ai/headroom | Apache-2.0 (~30% core) | (PART-B batch4-extra) REFERENCE — token-compression middleware; agent hyped "huge cost savings" but N/A to us NOW: our news prompts are small + we moved to FREE local Ollama (token-cost=0). +60MB onnxruntime/ModernBERT to compress small free prompts = bloat. Micro-gem (must-keep regex for keyword prune) = BACKLOG, no dep. | (none — REFERENCE) | not needed (small prompts, free-local) |
| 2026-06-30 | DeusData/codebase-memory-mcp | MIT (1.4GB) | (PART-B batch4-extra) SKIP — codebase knowledge-graph for IDEs/agents; wrong domain (we're a video factory, not a code-nav assistant). gitingest+markitdown already cover repo ingestion. | (none) | wrong domain |
| 2026-06-30 | mvanhorn/last30days-skill | MIT (1467-line core, 100%) | (PART-B batch6 → BUILT batch10) **ADOPTED & WIRED**: entity-resolution + multi-source recency ranking → `discovery._fetch_github_trending()` — auto-queries `gh` per AI/dev topic for repos created in last N days, sorted by stars → self-feeds the queue (no static list). Verified live: found 9 fresh trending repos. Toggle SEOSONA_TREND. | `4_BRAIN/discovery.py` (`_fetch_github_trending`) | FACTORY: autonomous content discovery |
| 2026-06-30 | RAG/PDF-infra group (chroma · MinerU · chunkr · PixelRAG · Hyper-Extract · opendataloader-pdf) | Apache-2.0 / mixed (README-scout) | (PART-B batch4) **all REFERENCE/SKIP** — vector-RAG / semantic-chunk / knowledge-graph / visual-retrieval / PDF-OCR infra that a faceless video factory doesn't use; markitdown+gitingest cover ingestion. Conditional backlog: MinerU OCR + opendataloader bounding-boxes IF we ever do infographics-from-PDF. No additive gem now. | (none — REFERENCE) | ingestion already covered |
| 2026-06-30 | AIDC-AI/Pixelle-Video | Apache-2.0 (23.8k★) | (PART-B batch8 "full sweep" — user "làm full, biết đâu mỏ vàng"; full-stack short-video engine) ~65% DUP w/ ours (two-stage, audio-sync, FFmpeg-concat, HTML-render, LLM-fallback) = confirms our architecture is sound. 1 cheap gem: **opening-diversity** (forbid same opening word ≥2×) → `_gemini_script` prompt. PipelineContext/Storyboard/FrameProcessor refactors = BACKLOG (don't refactor working code). Image-gen/ComfyUI/Streamlit = N/A. | `4_BRAIN/make_video.py` (_gemini_script) | CONTENT: less repetitive openings |
| 2026-06-30 | voocel/ainovel-cli | Apache-2.0 (Go) | (PART-B batch8 → BUILT batch10) 2 gems built: (1) deterministic **`_lint_script`** (CTA·length·forbidden·English-leak·opening-diversity·visual-monotony·**placeholder-echo**); (2) **critic-correct loop** — draft trips serious lint → regenerate ONCE with feedback, keep cleaner (bounded, keep-best guards bad retries). Coordinator/episodic-memory = still BACKLOG. | `4_BRAIN/make_video.py` (_lint_script + corrective loop) | CONTENT: self-correcting quality gate |
| 2026-06-30 | ComposioHQ/awesome-claude-skills + garrytan/gstack | Apache / MIT (catalogs) | (PART-B batch8 catalog scan) REFERENCE — gstack patterns (sprint-loop/review/investigate) mostly DUP w/ our LLM_CODING_DISCIPLINE+SIL+capability-analyst+loop_guard; nothing to build now. Pointers noted for later: rampstackco Brand-Build SEO skills + Content-Research-Writer (fact-check) → future video-seo-metadata/news-maker upgrade. No vendoring (gstack 108MB, NO-LICENSE catalog). | (none — REFERENCE) | discipline already covered |
| 2026-07-01 | alexchan197611/ai_media_assistant | **MIT** (193★, v2.0) | (user-flagged, 7-step gate) **REFERENCE — NOT adopted.** Closest sibling yet: local script→short-video for creators, **same voice engines (OmniVoice + Qwen3-TTS)**, FastAPI + separate render-worker + SQLite (UUID-per-sentence segment) + **FFmpeg** render. ~high DUP w/ our pipeline → confirms architecture sound; don't recreate (same call as [[Pixelle-Video]]). NOTE our HTML→browser render is a deliberate different path from their FFmpeg-concat. Gems: (1) **Qwen3-TTS** = a VN voice A/B candidate we don't have → REPO_WATCHLIST; (2) **per-sentence local-image-library matching** (emotion-based, no-repeat within project) + 4 caption styles (centered/scrolling/ancient/emotional) → IDEA-BACKLOG for a possible image-backed template archetype, GATED by brand (clean tech-editorial — same caution that rejected Pixelle's imagery). Impl internals not deep-read (README user-facing/Chinese); adoption unlikely per brand precedent — revisit only if image-backed template ever wanted. No code vendored. | (none — REFERENCE; watchlist for Qwen3-TTS) | closest competitor; validate architecture + harvest voice/caption ideas |
| 2026-07-02 | nexu-io/html-video (STUDIO) | Apache-2.0 | (user-flagged, screenshot of studio v0.7) **REFERENCE-pattern for a future dashboard "Studio/Create" view.** The render ENGINE lineage (hyperframes) is already OWNED (5_FRAMEWORK/hf_engine); what's NEW here is the browser STUDIO UI (`project-studio/`, served :3071): template gallery filterable by intent, frame-by-frame storyboard editor w/ inline per-frame KICKER/HEADLINE/SUB text binding, CLI-agent router, dual-audio panel (BGM mood-prompt + narration TTS), Export MP4, pluggable `render(input,ctx)` engine contract + content-graph IR. Same family + same engine + Apache-2.0 = the ideal UI blueprint. TENSION: faceless-autonomous vs human-frame-editing → adopt as an OPTIONAL human-oversight/create mode on the dashboard, not the autonomous default. Don't vendor the TS monorepo. | (none — REFERENCE; dashboard "Studio" view backlog) | best-fit UI blueprint for create/preview/edit |
| 2026-07-02 | nexu-io/html-anything | Apache-2.0 (7.5k★) | (user-flagged) REFERENCE — agentic HTML editor, sibling to html-video. Mineable: (1) 75 HTML skill-templates across 9 surfaces (incl. office: OKR/finance/kanban/runbook) → candidates for the component/template library + dashboard card layouts; (2) 8-CLI-agent auto-detection pattern; (3) export-to-social (WeChat/X/Zhihu/PNG/HTML). Overlaps our library system → mine patterns by hand, don't vendor Next.js. | (none — REFERENCE) | template + agent-detection patterns |
| 2026-07-02 | vanloctech/youwee | MIT (1.1k★) | (user-flagged) REFERENCE / mostly-SKIP — yt-dlp GUI (Tauri) + AI summarize + Whisper subs + Telegram + plugins. ~DUP with what we already have: [[video-pipeline-upgrades]] yt_downloader + source_footage + PhoWhisper asr_router + hermes telegram_remote. Only possible gem = its 1800-site source list. Nothing to build. | (none — SKIP-DUP) | download+ASR already covered |
| 2026-07-02 | OpenTalker/SadTalker | **Apache-2.0** (commercial OK, non-commercial restriction removed) | (user-requested) **ADOPTED → Engine #3: Portrait Avatar** (still photo + audio → lip-synced talking head). Audio-driven (correct for photo+voice; LivePortrait rejected = video-driven). Installed in ISOLATED venv `2_KNOWLEDGE/external_toolkits/.venv-sadtalker` (torch2.5+cu121, does NOT touch hermes env). Wrapper: `scripts/portrait_avatar.py` (photo+audio→clip, --still/--enhance/--size, auto 16k-wav). VERIFIED: art_10 photo + tabfm voice → 8s lip-synced clip (mouth+head animate, identity preserved). Weights ~1.2GB (256 model + mappings + gfpgan/facexlib) in SadTalker/checkpoints (gitignored). **Windows-modern-numpy fix chain (reusable):** (1) basicsr `torchvision.transforms.functional_tensor`→`functional`; (2) SadTalker src `np.float/np.int/np.bool`→`float/int/bool`; (3) coherent numpy<1.24 stack `numpy==1.23.5 scipy==1.10.1 scikit-image==0.21.0 (0.19.3 has no py3.11 wheel) scikit-learn==1.2.2 numba==0.57.1 llvmlite==0.40.1`. | `2_KNOWLEDGE/external_toolkits/SadTalker/` + `.venv-sadtalker/` + `scripts/portrait_avatar.py` | photo→lipsync talking-head (lecturer avatar) |
| 2026-07-02 | DanielSWolf/rhubarb-lip-sync | **MIT** (output is yours, commercial OK) | (user-requested) **ADOPTED → Engine #4: Talking Mascot** (2D brand mascot lip-syncs to a SEOSONA voice, NO GPU). Rhubarb CLI (`-r phonetic` = language-independent → VN-safe) → viseme timings → Pillow paints a viseme-driven mouth over the mascot's covered smile → frames+voice → mp4. Wrapper `scripts/mascot_talk.py`. VERIFIED: Chi Quyet mascot + tabfm voice → 8s talking clip (mouth opens/closes in sync, on brand-light bg). Fits faceless brand better than a photorealistic stranger. Rhubarb binary in `2_KNOWLEDGE/external_toolkits/rhubarb/` (88MB, gitignored). GOTCHA: rhubarb `-o` needs the output DIR to exist first ("ios_base::failbit" = can't write output). | `scripts/mascot_talk.py` + `2_KNOWLEDGE/external_toolkits/rhubarb/` | GPU-free talking brand mascot |
| 2026-07-02 | TMElyralab/MuseTalk | MIT | (user-requested, ATTEMPTED — **BLOCKED on Windows toolchain**) higher-quality/faster lip-sync than SadTalker (MIT, 4GB VRAM, 256px, Windows). Deps install OK (diffusers/transformers) but **mmcv (needed by mmpose/DWPose) has NO prebuilt wheel for torch2.5+cu121 → source build needs MSVC (not available)**. Unblock path = dedicated venv with torch2.1+cu121 (where mmcv 2.1 wheels exist) + MuseTalk weights (~several GB) + wrapper. DEFERRED — the 2 working engines (SadTalker photoreal + Rhubarb mascot) already cover both needs; MuseTalk = marginal upgrade, revisit if sharper lips wanted. | (repo cloned; not wired) | blocked: mmcv/Windows |
| 2026-07-02 | facefusion/facefusion · lipku/LiveTalking · hecomi/uLipSync | OpenRAIL-AS ⚠ / Apache / MIT | (6-repo lip-sync analysis) all NOT adopted. **facefusion** = face swap/enhance; OpenRAIL-AS use-restrictions = risk for commercial factory; GFPGAN (in SadTalker) already covers face-enhance → SKIP. **LiveTalking** = real-time STREAMING digital human; SEOSONA is batch pre-render, not livestream → SKIP (revisit for live/interactive). **uLipSync** = Unity-only (C#), wrong stack → SKIP. | (none — REFERENCE/SKIP) | analysis of 6 avatar repos |
| 2026-07-02 | KwaiVGI/LivePortrait | (analyzed) | REJECTED for this use — **video-driven** (needs a driving performance video), not audio-driven → wrong fit for photo+voice→lipsync. Revisit only if we want video-to-video face reenactment. | (none) | wrong modality |
| 2026-07-02 | nexu-io/open-design (re-dig) | Apache-2.0 | (user re-flagged) already INGESTED 2026-06-27 (HyperFrames craft → 2_KNOWLEDGE/hyperframes/craft/). Re-reference to harvest any NEW palettes/transitions/motion added upstream since. No new vendor. | `2_KNOWLEDGE/hyperframes/craft/` (existing) | craft source may have grown |
| 2026-07-01 | (user-flagged batch: 4 more repos triaged, README-scout) | mixed | **OmniRoute** (diegosouzapw, MIT, 8.9k★) = AI-gateway/router 236 providers + token-compression 15–95% + circuit-breaker. **BUILT the circuit-breaker gem** → `llm_engine._gemini_scenes` now skips a key that just hit 429/quota for a cooldown window (`SEOSONA_KEY_COOLDOWN_S`, default 300s) instead of re-hitting it first every call; ready keys tried first, tripped keys last-resort (never hard-fail). Test: `tests/test_llm_circuit_breaker.py` (4/4). Token-compression SKIPPED (small prompts + free Gemini/Ollama = cost ~0, same call as [[headroom]]). Gateway not vendored. **QwenPaw** (agentscope-ai, Apache-2.0, 20.4k★) = personal-agent platform; only gem = 3-layer memory (working/history/distilled) ≈ our memory → REFERENCE. **agency-agents** (msitarzewski, MIT, 122k★) = 232 personas; already in watchlist + made a video angle → REFERENCE (known). **local-coding-agent** (LongNgn204, **AGPL-3.0**, 11★) = MCP coding-agent for ChatGPT Web; wrong domain + copyleft → SKIP. | (none — all REFERENCE/SKIP) | triage of user-shared links |
| 2026-06-30 | batch8 MAP-skips (daily-news-broadcast · NVIDIA/vid2vid · NVIDIA VSS · cjpais/handy · nano-banana-prompts · openwolf · looper · x-harness) | mixed | SKIP/REFERENCE by-MAP (no agent spent): empty(0MB/4★) · GAN-video-gen off-domain · enterprise VSS NOASSERTION · STT (have PhoWhisper) · image-gen prompts 330MB · token-compress (decided N/A) · small verify-loops (have). | (none) | filtered at MAP |
| 2026-06-30 | ant-design/ant-design | MIT (React UI lib) | (PART-B batch6→batch8 CORRECTED — user: "video là HTML, design áp dụng được chứ?") React RUNTIME N/A, BUT design-CRAFT IS (components = HTML/CSS strings). MINED + ADOPTED (re-skinned brand, frame-verified): **layered 3-tier shadows** + **tabular-nums** + **star-pill shadow** → native_composer. Token-scale (radius/shadow/spacing seeds) = BACKLOG. Earlier "SKIP wrong-stack" was wrong. | `4_BRAIN/native_composer.py` (_css design-craft block) | FACTORY: premium card depth |

| 2026-06-30 | **NVIDIA/SkillSpector** | Apache-2.0 (11.5k★) | (PART-B batch9 "full S+A sweep" — THE gold of the sweep) ADOPT-pattern: skill/repo security scanner (68 patterns/17 cats). Re-implemented the cheap high-signal subset NATIVE (regex + simple AST, ~120 LOC, no-dep, offline) → **`scripts/security_scan.py`** (`npm run security:scan`). Catches prompt-injection · hidden-instruction (HTML/zero-width/unicode-tag) · secret-harvest · curl\|bash · b64-exec · os.system/subprocess · credential-access → score+SAFE/CAUTION/DO_NOT_ADOPT. Tested (bad=100/DO_NOT_ADOPT 9 findings, clean=SAFE). Full framework (MCP/Docker/LangGraph/YARA/taint) = BACKLOG. Wired into SIL stage-3 + capability-analyst. | `scripts/security_scan.py`, SIL stage-3, capability-analyst | SYSTEM: automated security gate (was manual) |
| 2026-06-30 | thedotmack/claude-mem | Apache-2.0 (85k★, ~15% core) | (PART-B batch9) SKIP-DUP/REFERENCE — Claude-Code IDE session-memory PLUGIN (hooks+daemon+Postgres+Chroma); over-build for headless factory. Core reflect/consolidate = already adopted from EverOS (stage-10); its "type taxonomy" = we already have (`type:` frontmatter). No new gem (85k★ ≠ fit). | (none — REFERENCE) | IDE plugin, not headless |
| 2026-06-30 | anthropics/claude-plugins-official + sickn33/antigravity-awesome-skills | Apache / MIT (catalogs, 31k+42k★) | (PART-B batch9 catalog scan) REFERENCE — official dir = IDE/LSP/dev tools (not video). antigravity 1700 skills: most DUP (eval/seo/content we have) or paid-service deps (Apify, Memories.ai, TranscriptAPI). Pointers noted for publish/discovery phase: youtube-seo-optimizer, seek-and-analyze-video, prompt-caching. No build now. | (none — REFERENCE) | pointers for publish phase |
| 2026-06-30 | batch9 MAP-skips (OmniClaw/NemoClaw/goclaw/hermes-agent etc. = agent-runtimes · skill-generator VN no-license · playwright-mcp · omni-agent · Vietnam_basemaps/timesfm/IPTV/games) | mixed | SKIP/REFERENCE by-MAP — agent-harness alternatives (we run on Claude Code, not adopting another runtime) + off-domain (geo/forecasting/IPTV/games). ~95 of 103 S+A off-domain filtered without spending agents. | (none) | off-domain / runtime-dup |

| 2026-06-30 | yusufkaraaslan/Skill_Seekers | MIT (14.3k★, 23MB) | (PART-B B-tier batch11) docs/repo→skill generator — mostly DUP (gitingest+markitdown+capability-analyst cover extraction). 1 cheap gem BUILT: **skill-quality linter** → `scripts/skill_lint.py` (`npm run skill:lint`) validates OUR skills' frontmatter/name/description/body. Tested: 43 skills all OK. Scan→bootstrap + AgentClient = REFERENCE/BACKLOG. | `scripts/skill_lint.py` | SYSTEM: skill-library gate |
| 2026-06-30 | Upload-Post/skill-autoshorts | **NO-LICENSE** (96★, full-read) | (PART-B B-tier batch11) daily viral-clip pipeline (Whisper+Gemini+Upload-Post) — thinner than ours, NO-LICENSE→pattern-only. HOT.md learning-flywheel → **BUILT FREE** (`4_BRAIN/learn_flywheel.py`, `npm run factory:learn`): uses free per-render signals (verify-pass + lint-warns) not publish-analytics → ranks templates, `_rot` auto-drops weak ones. OODA loop closed; cycle-pick ≈ our processed-ledger (DUP); hook-overlay = DUP (HTML captions); inline frame-QA = we have eval_judge/evaluator. No cheap gem to build. | (none — REFERENCE) | thinner/DUP, no-license |

| 2026-06-30 | Unclecheng-li/AI-Animation-Skill | MIT (453★, full-read) | (PART-B B-tier batch11) REFERENCE/SKIP — HTML presentation SLIDES via CSS @keyframes (wall-clock) → NOT seek-safe for our paused-GSAP render. Motion patterns (orb-float≈our scblob, pulse-glow≈our scglow, explode/shake=off-brand for pro tech-news) = DUP/off-brand. No buildable gem. | (none — REFERENCE) | CSS-keyframes slides, not seek-safe |
| 2026-06-30 | **C-tier sweep (741 repos, lowest 'Backlog' tier)** | mixed (scout, no clone) | (PART-B final) **DRY** — top-25 relevant scouted: 15 off-domain (medical/tax/git-config/scrapers/TV-clients), 5 DUP (TTS/voice we have), 5 marginal (China-centric kits). 0 genuine gems. 1 marginal REFERENCE: **Huanshere/VideoLingo** (17k★, subtitle/translate/dub pipeline) — revisit ONLY if multilingual/dubbing expansion. Inventory mining CLOSED. | (none) | vein exhausted after S+A+B |
| 2026-06-30 | nateherkai/hyperframes-student-kit | NOASSERTION (271MB, MAP) | (PART-B final keyword-sweep — last repo with any video-domain keyword) REFERENCE — HyperFrames learning kit; we already OWN the canonical engine (5_FRAMEWORK/hf_engine) + 97-block registry + open-design craft library. Derivative examples, can't-vendor (no-license), C-tier. Skim only if we ever want more template samples. | (none) | we own the source engine |
| 2026-06-30 | GuanYixuan/pyCapCut + pyJianYingDraft | Apache-2.0 (3800★/570★) | (user-led: "CapCut có nhiều SFX/transition/text-effect") **ADOPTED+BUILT** `4_BRAIN/capcut_export.py` (`npm run capcut:export`, opt-in `SEOSONA_CAPCUT_EXPORT=1`): SEOSONA MP4 → editable **CapCut draft** so user embellishes with CapCut's native effect library. Uses pyCapCut (intl CapCut; pyJianYingDraft = China-only, skipped). Verified: generates valid draft (format version 360000 = matches machine's CapCut 8.9; clean schema-subset). CAVEAT: GUI-open in CapCut 8.9 NOT visually confirmed (computer-use was declined) — static evidence strong. | `4_BRAIN/capcut_export.py` | FACTORY: hand off CapCut project, not just MP4 |
| 2026-06-30 | **CapCut ecosystem deep-dig (6 repos)** — capcut-cli (MIT 124★), VectCutAPI (Apache 2k★), capcut-mate (Apache 1.3k★), opencut (MIT 60k★), capcut-ai-editor (MIT MCP), pyJianYingDraft | mixed (clones, deep-read) | (user-led: "đào vàng kỹ hơn") **Hard finding: NO free+headless CapCut-effect render exists** — every path = CapCut GUI (manual/RPA-Windows-fragile) or PAID cloud (VectCutAPI license-gated). So harvest *knowledge*, apply natively. **ADOPTED+BUILT:** (1) SFX de-click (afade in/out, ffprobe-cached); (2) **scene-entrance transition variety** — `_entrance_tweens` rotates 4 brand-fit recipes/scene (slide-up / from-right / zoom-dissolve+headline-clip-wipe / from-left), mirrors the existing `_EXITS` rotation, seek-safe GSAP, frame-verified (5-scene render: all resolve to rest, text fully visible, no black frames). Done in the seek-render — NOT ffmpeg xfade (our render is one continuous seek-render, not concatenated clips). Both in `native_composer`, free/headless, **no CapCut**. **BACKLOG:** SFX sidechain-duck; more text-effect variety (word stagger / scale-pop); subtle flash accent; 6 re-skinned text templates; talking-head silence/dupe-detect + translate. **SKIP-DUP:** caption/karaoke (ours is Vietnamese-grammar smarter). **SKIP:** ~85% of effect catalog (anime/neon/retro/cute/dark = off-brand). capcut-cli verified on machine (doctor: detects CapCut 8.9 drafts + ffmpeg + whisper; tested on CapCut 8.0–8.9 = resolves pyCapCut 6.7 open-uncertainty if we pivot the draft export later). | `4_BRAIN/native_composer.py` (afade), `2_KNOWLEDGE/VIDEO_CRAFT_RULES.md` §8 | knowledge → native engine; CapCut = handoff only |
| 2026-07-01 | **Knowledge-graph/"second brain" scan (user-led)** — AgriciDaniel/claude-obsidian (Karpathy's "LLM Wiki" pattern), breferrari/obsidian-mind, tirth8205/code-review-graph, mmethodz/dreamgraph, kepano/obsidian-skills, safishamsi/graphify, Lum1104/Understand-Anything, abhigyanpatwari/GitNexus | mixed (README-scout, no clone; found via repo_inventory xlsx keyword search) | (user: "Second Brain không còn chỉ lưu trữ mà thành hệ thống AI làm việc cùng bạn — Obsidian, Karpathy Method") REFERENCE-pattern only (license mixed/unclear, none free-local-fit as-is) — the SHARED insight across all of them: a knowledge graph should be QUERYABLE DATA an agent reads to work faster (code-review-graph claims 6.8–49× fewer tokens), not just a picture for humans. **ADOPT-pattern → BUILT natively**: `scripts/gen_knowledge_graph.py` (scans the real repo: SOPs/4_BRAIN modules/agents/skills/templates/components/effects → emits `2_KNOWLEDGE/knowledge_graph.json`) + `4_BRAIN/knowledge_graph.py` (query API: `find/neighbors/related_to/stats`) so capability-analyst/factory-strategist can traverse system structure instead of re-reading the whole repo each time. Fixes the earlier gap (SYSTEM_MAP.html graph was a hand-written static snapshot). **Extended (content layer):** `scripts/gen_knowledge_notes.py` → `2_KNOWLEDGE/knowledge_notes.json` (134 notes from INGESTION_LOG lessons + VIDEO_CRAFT_RULES + domain_skills + SOPs + flywheel) + `knowledge_graph.recall(query)` — agent retrieves what SEOSONA has LEARNED + WHY (in our own words), cross-linked to structure nodes. This is the "AI answers using YOUR knowledge" half (Karpathy/Obsidian second-brain). Also added `health`/`learned`/`orphans` queries + generator auto-syncs into SYSTEM_MAP.html. Wired into capability-analyst step 0 + factory-strategist. | `scripts/gen_knowledge_graph.py`, `scripts/gen_knowledge_notes.py`, `4_BRAIN/knowledge_graph.py`, `2_KNOWLEDGE/knowledge_graph.json` + `knowledge_notes.json` | SYSTEM: graph as living queryable data + content recall, not just a diagram |
## Rejected (logged so we don't re-evaluate)
| Source | Why rejected |
|--------|--------------|
| fishaudio/fish-speech | non-commercial license + torch 2.8 (breaks env) |
| coqui-ai/TTS | unmaintained (shut down) + no Vietnamese |
| RVC-Boss/GPT-SoVITS, OpenMOSS/MOSS-TTSD, voice-pro, voicebox | no Vietnamese |
| K07VN/capcut-tts-api | no LICENSE + reverse-engineered cloud API |
| WEIFENG2333/VideoCaptioner | GPL-3.0 |
| social-auto-upload, opencut, palmier-pro, capcut-cli, openreel | whole apps/editors or no license — reference only |
| ToolJet, Prometheus | whole low-code/monitoring platforms — over-engineer a local log dashboard |
| firecrawl, browser-use, Scrapling | heavy crawl/browser frameworks — native RSS fetch (requests+xml) is the right size |
| MoneyPrinterTurbo, KrillinAI, autoshorts | full video pipelines — would conflict with native_composer (don't rebuild) |
| gsap/anime/lottie skills (boraoztunc) | duplicate — SEOSONA already has the HyperFrames craft library |
| awesome-seo (awesomelistsio/teles), awesome-ui (kevindeasis), awesome-digital-marketing (paulbradish) | **no LICENSE file** → not vendored (SOP step 2); referenced by link in `domain_skills/INDEX.md` |
| 2026-07-02 | coreyhaines31/marketingskills | MIT (48 skills, markdown) | **ADOPTED (narrow)** — only the 2 genuinely-new craft pieces (the other ~46 duplicate our 17 domain_skills): (1) **4 named HOOK types** (Curiosity/Value/Story/Controversial) + phrasing templates + Story-Arc/POV structures; (2) **marketing-psychology** — ~15 named persuasion levers for a 45-60s short (Curiosity-Gap/Zeigarnik/Loss-Aversion/Social-Proof/Scarcity/Peak-End/Reciprocity), HOOK-lever + CTA-lever, each must ride a REAL fact. | `2_KNOWLEDGE/domain_skills/marketing-psychology/SKILL.md` + `.../copywriting/references/short-form-structures.md` → folded into `MASTER_VIDEO_SPEC §3b` (writer receives it via `_spec_excerpt`) | sharper, named hooks + ethical persuasion levers for `script_writer` |
| 2026-07-02 | nidhinjs/prompt-master (re-dig) | MIT | REFERENCE — core already ADOPTED 2026-06-30 (grounded `_gemini_script`). New-but-not-adopted: 37-item prompt anti-pattern checklist (one-time audit lens for stage prompts). No vendor. | `2_KNOWLEDGE/REPO_WATCHLIST.md` (link) | avoid a parallel prompt system; MASTER_VIDEO_SPEC is the single system prompt |
| 2026-07-02 | Souvic/videodubber · HITsz-TMG/VideoClaw · nazdridoy/kokoro-tts · google-research/tabfm | GPL-3.0 / MIT / MIT / Apache-2.0 | **SKIP** — videodubber=GPL+paid remote API (no VN proof); VideoClaw=100% paid cloud gen-video (breaks free-local rule); kokoro-tts=English model already dropped (REFERENCE only for its chunk/merge CLI ergonomics on voice_router); tabfm=off-topic tabular ML. | (none) | vetting gate filtered — paid/off-topic/English/dup |
| 2026-07-02 | nexu-io/html-video · vanloctech/youwee (re-flagged) | Apache-2.0 / MIT | already logged 2026-07-02 (html-video=REFERENCE Studio-UI backlog; youwee=SKIP-DUP download+ASR+telegram). No new action. | (existing) | re-confirmed |
| 2026-07-02 | nazdridoy/kokoro-tts (deep re-dig) | MIT | **BUILT** (was SKIP as engine — user pushed "đào sâu"): ported its **adaptive long-text chunking** (period-split → greedy ~1000-char → word-split oversized → **error-driven shrink retry** at 60%) into `omnivoice_engine.synthesize()`. OmniVoice previously synth'd the WHOLE script in one call (truncation/timeout risk on long/course scripts). Short scripts stay 1 call (unchanged). | `2_SKILLS/voice_cloner/omnivoice_engine.py` (`_chunk_text`/`_synth_one`/`_concat_wavs`) | long-script TTS robustness |
| 2026-07-02 | GSAP SplitText (greensock/GSAP) | GreenSock no-charge (free commercial, permissive-in-practice — flagged) | **BUILT** — per-CHARACTER kinetic typography. GSAP 3.14 already loaded; SplitText now free (3.13+). New `char-cascade` text-effect (seek-safe `tl.from`, GUARDED: falls back to whole-headline rise if plugin absent → never breaks). SplitText CDN + `registerPlugin` added to the render doc. | `4_BRAIN/effect_library.py` (`_te_char_cascade`) + `native_composer.py` (CDN) | biggest visible-quality jump, ~0 cost |
| 2026-07-02 | AgriciDaniel/claude-youtube | MIT (224★, 5.3k lines md) | **ADOPTED (narrow)** — retention science: value-prop in first ~5s, **pattern-interrupt every 2-3s (Shorts)**, gradual-slowdown energy, benchmark ~70%+ retention @30s. Folded into `MASTER_VIDEO_SPEC §3` (writer-visible). Thumbnail packaging (5-words-max, 1 focal point, 30-40% negative space, no title↔thumbnail dup) → frame-0 rule. | `9_PROMPTS/MASTER_VIDEO_SPEC.md` §3 + frame-0 | sharper retention + packaging rules |
| 2026-07-02 | zeropointnine/tts-audiobook-tool · loserbcc/open-unified-tts · benbrandt/text-splitter | MIT / Apache-2.0 / MIT | REFERENCE (backlog for the chunk stitch): variable-pause at sentence/paragraph boundaries + silence-trim (tts-audiobook, natively supports OmniVoice); **crossfade 30-50ms stitch** to kill chunk-seam clicks (open-unified-tts); Unicode VN sentence splitter fallback (text-splitter). | (backlog → `omnivoice_engine` concat) | polish the chunk stitch when needed |
| 2026-07-02 | dashed/whisperx-subtitles · ozten/CaptionsPlease · HITsz-TMG/VideoClaw | no-license / MIT / MIT | REFERENCE: caption cue-duration by **words-per-second** readability floor (whisperx); **LLM emphasis-word** auto-highlight 15-25% (CaptionsPlease → future script_writer→karaoke); editable plan-JSON checkpoints (VideoClaw → partial-rerender roadmap). videodubber re-confirmed **NOTHING** (paid client, all timing server-side). | (backlog notes) | small future enhancements |
| 2026-07-02 | nghimestudio/vietnormalizer | MIT (zero-dep pure Python, 17K dict) | **BUILT** — VN text→spoken normalization (dates/%/currency/units/phone/ordinals) into `native_composer.pron()`. Closes a real voice gap: `_say_number_vi` only rounded bare thousands; now "47%"→"bốn mươi bảy phần trăm", "50.000đ"→"năm mươi nghìn đồng", "25/12/2025"→spoken date. Lexicon runs FIRST (SEO→"séo" protected from its Latin-transliteration); best-effort with `_say_number_vi` fallback. `pip install vietnormalizer`. | `4_BRAIN/native_composer.py` (`_vn_normalize` + `pron()`) + requirements.txt | voice reads %/dates/currency/units correctly |
| 2026-07-02 | amplifying-ai/awesome-generative-engine-optimization | **CC0-1.0** (public domain) | **ADOPTED** → domain_skill #19 `geo-aeo/SKILL.md` (distilled): GEO vs SEO, be CITED by ChatGPT/Gemini/AI-Overviews, answer-first + RAG-chunk-ready + llms.txt + entity trust; real 2026 signals (AI Overviews ~half of queries, Wikipedia ~48% of ChatGPT citations, LLM traffic ~6× convert). Checklist depth from Auriti-Labs/geo-optimizer-skill (MIT). A distinct 2026 discipline our 4 SEO skills predate. | `2_KNOWLEDGE/domain_skills/geo-aeo/SKILL.md` + INDEX | on-trend SEO-video content grounding |
| 2026-07-02 | stroke-dashoffset chart strategy (react-minimal-pie-chart MIT · Peity MIT · CSS-Tricks progress-ring) | MIT / technique | REFERENCE→BUILD-next: single-numeric-property seek-safe `fromTo` draws NEW chart archetypes we lack — **ring/gauge · donut/pie · line-chart** + a **timeline** component. Pattern only (all sources are React/jQuery wrappers). Needs a render test (SVG compositing in HyperFrames). | (backlog → `native_composer._component` new kinds) | richer data-viz for SEO stat videos |
| 2026-07-02 | Pixabay API (video+music) · Openverse (MIT) · trungtv/pyvi (MIT) | Pixabay Content License / MIT / MIT | REFERENCE→BUILD-next (needs a free Pixabay key): **B-roll VIDEO** source (real gap — we only source photos) → new `2_SKILLS/broll_sourcer/` mirroring image_sourcer; Pixabay Music (no-attribution) to refresh `BGM` mood files; Openverse `/v1/audio?license=cc0` = programmatic safe-BGM. pyvi = VN word-boundary for caption line-breaks (secondary). Jamendo/Unsplash/FreePD = SKIP (non-commercial / attribution / dead). | (backlog → broll_sourcer, BGM, caption_segment) | b-roll video + fresh safe BGM |
| 2026-07-02 | NVIDIA NIM API (build.nvidia.com) — user key | free tier (1-5k credits, ~40 RPM) | **BUILT (2)**: (1) LLM cascade tier `_nvidia_scenes` (Llama-3.3-70B / Qwen2.5-72B, OpenAI-compat `integrate.api.nvidia.com/v1`) inserted after Z.ai — AUTH VERIFIED (llama-3.1-8b replied VN); 70B cold-starts slow → 150s timeout, it's a fallback. (2) `4_BRAIN/image_gen.py` FLUX.1-schnell (NIM-native `ai.api.nvidia.com`, base64 artifacts, `cfg_scale=0` required) — fills the no-image-gen gap for thumbnail art + synthetic bg; endpoint/auth/payload VERIFIED but free-tier gen queues >180s (may need 202-async poll) → built + safe-fallback, use when tier warm. | `4_BRAIN/llm_engine.py` + `4_BRAIN/image_gen.py` + .env | free LLM fallback + image-gen |
| 2026-07-02 | Pixabay (user key) | Pixabay Content License (commercial, no attribution) | **BUILT** — `2_SKILLS/broll_sourcer/` mirrors image_sourcer for **B-roll VIDEO** (real gap: we only sourced photos). Pixabay video API → download → ffmpeg center-crop 9:16 → cache+learn; 429-backoff. VERIFIED: "làm việc" → 1080×1920 clip. Music = curated download to grow `native_composer.BGM` (backlog). | `2_SKILLS/broll_sourcer/broll_sourcer.py` | b-roll video per scene |
| 2026-07-02 | stroke-dashoffset ring (react-minimal-pie-chart MIT · CSS-Tricks) | MIT / pattern | **BUILT** — new `ring` component (circular % gauge, SVG stroke-dashoffset, seek-safe static-reveal) in `native_composer._component` + CSS + effect_library `_KIND_CLASS`. VERIFIED HTML gen (80%→offset correct). Needs a render test (SVG compositing in HyperFrames). donut/timeline/linechart = backlog. | `4_BRAIN/native_composer.py` (`ring`) | circular data-viz for stat videos |
| 2026-07-02 | publishing (gitroomhq/postiz AGPL · XavierZambrano/facebook-reels-api MIT) | AGPL(ref) / MIT | REFERENCE + knowledge: our `_to_tiktok` never completes (init only, no upload+poll) & `_to_facebook` uses legacy `/videos` not Reels 9:16. Verified 2026 platform specs → `2_KNOWLEDGE/distribution/PLATFORM_SPECS.md`. Code fixes = backlog (need real tokens + platform audit). Cookie uploaders = SKIP (ToS/ban). | `2_KNOWLEDGE/distribution/PLATFORM_SPECS.md` | fix publish routes when tokens ready |
| 2026-07-02 | Google Trends "Trending Now" RSS + Autocomplete (keyless) | n/a (Google endpoints) | **BUILT** — `researcher.trends_vn()` (real VN search-DEMAND queries + traffic band + linked news, keyless RSS like news_rss) + `autocomplete()`/`expand_keywords()` (Google+YouTube long-tail queries people TYPE; YT JSONP handled). Fills a real gap: research had news-discovery but no search-DEMAND → topic selection can now be demand-driven + traffic bands are citable. VERIFIED live: trends "viên chức [5000+]", YT "marketing căn bản/là gì". | `4_BRAIN/researcher.py` (`trends_vn`/`autocomplete`/`expand_keywords`) | demand-driven topics |
| 2026-07-02 | GSAP SplitText mask API + kinetic recipes (zarazhangrui templates MIT = REFERENCE) | free GSAP 3.13+ / MIT | **BUILT** — `line-mask` text-effect (per-line slide from behind an overflow-clip mask, SplitText `mask:"lines"`, guarded fallback, seek-safe) into effect_library. Backlog: highlight-sweep (needs .hl-bar markup), odometer digit-roll (seek-safe bignum), `divider` section-card archetype. zarazhangrui slide templates = REFERENCE (layout ideas; @keyframes/dark → don't vendor). | `4_BRAIN/effect_library.py` (`_te_line_mask`) | richer kinetic typography |
| 2026-07-02 | stroke-dashoffset timeline (react-minimal-pie-chart pattern) | pattern | **BUILT** — `timeline` component (dated milestones on a spine, normal-flow + .ritem reveal, NO SVG = lowest render risk) in `native_composer._component` + CSS + reveal-count + KIND_CLASS. VERIFIED HTML gen (3 rows/spine/reveal). | `4_BRAIN/native_composer.py` (`timeline`) | roadmap/history data-viz |
| 2026-07-02 | Netflix/BBC timed-text standards + chetan-prime/subfixer (REF) | web standard / free | **BUILT** — `caption_segment.readability_warnings()` (deterministic caption-readability lint: CPS≤20, ≤42 char/line, on-screen ≥0.83s — flags cues too fast/long to read). Native, no dep, matches `_lint_script`/`spec_lint` pattern. VERIFIED (37 CPS cue flagged; comfortable cue passes). Wiring into build_ass/native_composer caption step = 1-line connect (backlog). | `4_BRAIN/caption_segment.py` | readable captions = better retention |
| 2026-07-02 | section-card `divider` component | own build | **BUILT** — `divider` component (oversized index + label for "Phần 1/Bước 2" multi-part videos) — a scene archetype we lacked. Normal-flow, KIND_CLASS=hook (punchy). VERIFIED HTML gen. | `4_BRAIN/native_composer.py` (`divider`) | multi-part video sections |
| 2026-07-02 | keplerlab/katna (MIT) + YouTube-Analytics own-channel + bgalbraith/bandits (Apache) | MIT/Apache | REFERENCE→BACKLOG (need OpenCV / real posting): (1) Katna best-cover-FRAME scorer (Laplacian-blur+brightness+entropy+LUV-dedup) → port native into thumbnail_maker (avoid pip, Py3.6-3.9 cap); (2) YouTube Analytics own-channel **retention curve + thumbnail-CTR** (free 2026-01-15) → `performance_tracker.py` feeding `learn_flywheel` (needs the posted `video_id` join-key — `record(**row)` already accepts it, publisher must pass it); (3) Thompson-Sampling bandit (~15 LOC) for hook/thumbnail selection. All need real posting/GPU → backlog. | (backlog) | close the performance feedback loop |
| 2026-07-03 | hoquanghai/Auto-Create-Video (HyperFrames VN sibling) | MIT | **MOSTLY-CONVERGED** (deep-mined — a near-exact TS twin of our faceless engine: article→9:16 via HyperFrames+GSAP). Its headline craft is ALREADY in our factory: semantic SFX picker (`_semantic_sfx`, adopted earlier from AI-auto-generate-video), md5-deterministic BGM, voice-driven scene timing, Ken Burns, SFX-tail de-click, zod-schema≈`spec_lint`. **BUILT the one genuine gap:** `shimmer` text-effect (light sweep across headline; GSAP CSS-var tween = seek-safe, NOT their CSS-`infinite` which freezes under HF seek; rise-in + sheen; render-verified GPU-free, 531 frames clean). Grain-overlay = documented opt-in (global look change = brand call, not auto-on). ElevenLabs/LucyLab/TS-runtime = SKIP. | `4_BRAIN/effect_library.py` (`_te_shimmer`) | headline polish + convergence confirmation |
| 2026-07-03 | kadevin/ilab-gpt-conjure (GPT-image-2 WebUI) | AGPL-3.0 | **REFERENCE** (AGPL = no vendoring; our doctrine = REAL photos via image_sourcer, not paid GPT-image gen). Transferable-only: auth-retry + provider-fallback (401/quota cycling) and an asyncio.Semaphore concurrent-task+cancellation pattern → note for `llm_engine.py`/factory-loop IF we ever scale multi-key/multi-provider. Our LLM cascade + image_gen already cover the need. prompt_guard = Chinese + GPT-image-specific = N/A. No build. | (reference notes) | future multi-provider hardening only |
| 2026-07-03 | topviewai/ai-video-editor | (marketing) | **SKIP** — pure product-marketing README for TopView.ai (m2v 3-step flow images); "API: Release soon"; NO code. Only idea = their viral-hooks 3-level taxonomy (a web dashboard, not in repo) — we already have hook recipes ([[master-video-spec]] §3 + A7 ending/hook gate). Nothing to adopt. | — | none |
| 2026-07-03 | topviewai/skill | Apache-2.0 | **REFERENCE/SKIP** — a clean Python client for TopView's **paid credit-based** API (video i2v/t2v/omni, avatar4, TTS, remove_bg — all server-side gen). Doctrine mismatch: we are SELF-HOSTED (own HyperFrames + OmniVoice + real photos), don't call paid gen APIs. Its one reusable pattern = `client.poll_task` (submit→poll→success/failed, 600s timeout) which our render/broll_sourcer/image_gen already do. `submit`/`query`/`estimate-cost` CLI ergonomics = nice but N/A. No build. | (reference) | none (paid-API client) |
| 2026-07-03 | topviewai/1000-AI-collection-tools | (list, MIT) | **REFERENCE** (discovery directory) — an awesome-list of ~1000 AI tools. Mined TTS/Video/Translation/SEO/Music categories: all entries are **commercial SaaS** (ElevenLabs, Jasper, Murf, DeepL…); the ✅ column is NOT "open-source" (commercial waitlist tools carry it too). Zero self-hostable/adoptable code. Keep as a link directory only. | — | none |
| 2026-07-03 | LingyiChen-AI/AIComicBuilder | Apache-2.0 | **REFERENCE** — Chinese 漫剧 (comic-drama) generator: script→character-extract→AI-gen scene frames→**Seedance 2.0 (paid i2v)** animation. `hyperframes/` = just their promo mp4, not an engine. Paid-AI-gen + comic genre ≠ our clean-graphic SEO news. Idea noted: per-scene emotion-analysis/emotion-curve → but we already have director.py motion + `_semantic_sfx` (emotion-keyword→SFX) + BGM swell. No build. | (reference) | none (paid AI-gen, off-genre) |
| 2026-07-03 | alexmacarthur/typeit | **GPL** | **REFERENCE** (copyleft = no vendoring). Typewriter JS lib; we already have terminal-typing + `clip-wipe` (seek-safe typewriter-equivalent) + `char-cascade`. Technique known. No build. | — | none |
| 2026-07-03 | aagarwal1012/Animated-Text-Kit | MIT (Dart/Flutter) | **REFERENCE** — text-animation recipe catalog (colorize/wavy/flicker/scramble/liquid-fill/typer). Dart (unusable); recipes are gimmicky/off clean-light-brand or not seek-safe (wavy/flicker/scramble = looping/random → freeze under HF seek). Our 8 seek-safe text-effects (rise/word-up/char-cascade/line-mask/clip-wipe/word-pop/drop/shimmer) already cover the on-brand set. Adding more = brand-dilution (no-bloat). Build a specific one only on user request. | — | none (mature already) |
| 2026-07-03 | timoncool/videosos | (open, Remotion) | **REFERENCE/SKIP** — browser AI video editor (Remotion + FFmpeg.wasm) integrating **paid fal.ai + Runware.ai** (Veo/FLUX/Imagen). The value IS the paid models → doctrine mismatch (we're self-hosted + real photos). VIDEO_MODELS_AUDIT.md = a paid-model landscape doc (knowledge only). No build. | — | none (paid models) |
| 2026-07-03 | CameronFoxly/Ascii-Motion | MIT + Proprietary (dual) | **SKIP** — ASCII-art animation editor. Off our clean-light professional brand (ASCII = novelty). Premium-tier code is proprietary. Nothing on-brand to adopt. | — | none (off-brand) |
| 2026-07-03 | hans1801/hans-n8n · workflows/videos-stickman | (n8n workflows) | **SKIP** — n8n automation for stickman YouTube videos (Gemini agent → structured-output → aggregate → render). n8n orchestration (we're Python) + stickman genre ≠ our brand. Pipeline shape (LLM→script→assets→render) we already have. No build. | — | none (n8n + off-brand) |
| 2026-07-03 | heygen-com/hyperframes (OUR render engine) | (engine we depend on) | **MINED + BUILT (small)** — audited our own engine (installed v**0.7.24**; repo docs were a newer version so flags re-verified against the real CLI). Finding: engine is very capable, we use it well; most features (batch/composition-variables, `beats` music-sync, 4K supersample, `data-color-grading`, nested-composition relative timing, cloud/lambda) **don't fit our model** (single speech-synced SEO video, light brand) or add complexity. **BUILT:** render now honours `SEOSONA_RENDER_QUALITY` (draft/standard/high) + `SEOSONA_RENDER_GPU=1` (NVENC) + `SEOSONA_RENDER_WORKERS` — configurable, default = current behaviour (standard, no GPU → respects GPU budget). Capability map banked in `VIDEO_CRAFT_RULES §11`. Verified-real flags via `render --help`. | `4_BRAIN/native_composer.py` (render knobs) | render flexibility + engine map |
| 2026-07-03 | remotion-dev/remotion + remotion-dev/skills + docs/assets | remotion (open) | **REFERENCE** — Remotion is the React→MP4 engine that SKILL AUTO / videosos / AIComicBuilder use; we deliberately use HyperFrames instead. Its craft `rules/*.md` (audio, captions, sequencing, effects, light-leaks, audiogram) all require Remotion's WebGL/React packages (`@remotion/effects`, `useWindowedAudioData`) = NOT usable in our HTML+GSAP stack. Ideas (light-leak overlay, audio-waveform viz) = niche polish; the on-brand craft we already have. assets doc = "preload before render" (HyperFrames already does). No build. | — | none (different engine) |
| 2026-07-03 | heygen-com/hyperframes — DEEP dive (core engine, user-requested "đào thật kỹ") | (our engine) | **MINED EXHAUSTIVELY (4 agents) + BUILT 1.** Enumerated 203 registry + 109 catalog blocks, 24 engine SKILLs (incl. video-type recipes), the remotion-to-hyperframes guide, media/audio. Findings: **(a) Registry/catalog = confirmed hardcoded DEMOS** (only 6 color-parametric, all off-brand WebGL/heavy) → validates [[registry-blocks-are-demos]], SKIP. **(b) Video-type skills** (music-to-video/slideshow/pr/product-launch/podcast) = whole new video PRODUCTS = scope-expansion (user's call, not auto-built); a few writing rules notable. **(c) Animation techniques** = mostly seek-safe things we have (SplitText/clip-path/stagger); **BUILT the top one: karaoke "ASR word-pop"** — the SPOKEN word now pops (scale 1.12 + accent glow envelope) not just an instant colour flip. Seek-safe (GSAP), reflow-safe (span→inline-block). Render-verified GPU-free (evaluator PASS, "ưu" emphasised, no layout break). **(d) audio-reactivity** (`extract-audio-data.py`) = native HF feature, niche for voice content = REFERENCE. Engine-capability map already in VIDEO_CRAFT_RULES §11. | `4_BRAIN/native_composer.py` (karaoke word-pop) | narration readability + full engine map |
| 2026-07-03 | Remotion portability — SETTLED via remotion-to-hyperframes skill | (official guide) | **DEFINITIVE: nothing to port.** The engine's own `remotion-to-hyperframes` guide: sequencing/timing(interpolate/spring/Easing)/transitions/audio/img/fonts/colors all map to what we ALREADY do natively (GSAP + HTML tags + stroke-dashoffset). React state / React-UI / Lambda don't translate. It **explicitly says NOT to port `@remotion/paths` or `@remotion/shapes`** — HF's stroke-dashoffset (which we use) is simpler + covers it. Verified: fetched @remotion/paths (zero-dep) + shapes docs — portable but would DUPLICATE ours. Only genuinely-new = audiogram/audio-reactive (niche). Remotion = REFERENCE, closed. | (reference) | none (already covered) |
| 2026-07-03 | heygen-com/hyperframes — DEEPER dive (read primary sources, user: "bạn có thực sự hiểu?") | (our engine) | **UNDERSTOOD FIRSTHAND + BUILT 1 big.** Read myself: DESIGN.md (= their DOCS-SITE style guide, not the video system), the **`hyperframes-creative` FRAME-preset system** (13 FRAME.md design-token identities incl. blue-professional/coral — colors+type-ramp+cards in **`cqw` container-query units**, principle "atoms sacred · composition free · numbers from the script"), and **music-to-video** (`analyze-beatgrid.py` librosa → audiomap: energy/onsets/hard_stops/key_moments/phrases; "trust the analyzer"; pacing=beat_cut vs phrase_flow; ~40 motion-primitives). **KEY BUILD (answers user's "nội dung/icon bám sát voice hơn"):** faceless component-item reveals were EVEN-spread; now **voice-synced** — `_item_reveal_times` + `_comp_item_texts` find WHEN each item's keyword is spoken (reuse beat_timing) and reveal it then (visual .ritem tween + per-item SFX BOTH use the same times), even-fallback for unmatched. Unit-verified (items 0.94/2.54/4.14 = as-spoken vs even 0.85/1.4/1.95) + render-verified GPU-free (evaluator PASS, frame shows item 2 correctly still hidden while item 1 revealed). music-cut/beat-SFX = considered but deferred (BGM loop/medley→timeline mapping fragile for subtle gain; librosa IS available if pursued). cqw-typography + frame-preset system = noted (big CSS refactor, our fitH covers it). | `4_BRAIN/native_composer.py` (`_item_reveal_times`/`_comp_item_texts` + both reveal sites) | content builds as narrated — tighter voice-sync |
| 2026-07-03 | greensock/gsap + greensock/gsap-skills | no-charge (free) | **REFERENCE + unlock noted.** Since GSAP 3.13 (Webflow) **ALL plugins are FREE** — we load gsap+SplitText from CDN; also available now: **MotionPath** (flow tokens along edges/lines — motion we lack), DrawSVG (cleaner SVG draw vs our stroke-dashoffset), CustomEase (§5 brand-ease presets), MorphSVG, Flip, ScrambleText, TextPlugin. All seek-safe, CDN-loadable like SplitText. Highest-value future add = MotionPath (data-flow on concept_build/hub/linechart). Not built this pass (enhancement, not gap). ScrollTrigger/Draggable/Observer = interactive, N/A to seek render. | (noted → future MotionPath) | free GSAP plugin unlock |
| 2026-07-03 | ray-project/ray + heygen-com/ray | Apache-2.0 | **REFERENCE (infra, future scale).** Ray = distributed-compute framework for scaling Python workloads across a cluster; heygen fork drives distributed HyperFrames renders (their `examples/aws-lambda|gcp-cloud-run|k8s-jobs`). Relevant ONLY if we scale to a render FARM (many videos in parallel across machines). Our single-box factory + `--workers` local parallelism suffices now. Not video craft. | — | future render-farm scaling |
| 2026-07-03 | HyperFrames music-to-video + GSAP MotionPath → BUILT (user-requested 3) | (our engine) | **BUILT 2 of the 3 user asks + verified:** (1) **beat-aware SFX** (`_snap_cues_to_beats` in native_composer, librosa onset-detect on the ACTUAL BGM/medley, tiles a looped track's onsets across the video, nudges ONLY transition whooshes onto the nearest onset within ±0.22s — voice/scene timing untouched; env `SEOSONA_BEAT_SFX=0` opt-out; best-effort fallback). Verified on real BGM (cue 5.30→5.46 snapped, others kept). (2) **MotionPath data-flow** — GSAP MotionPathPlugin (free since 3.13) added to the render doc; concept_build now flows a glowing accent token A→B along each edge after the nodes build (seek-safe x/y tween; plugin loaded for future curved linechart flow). Render-verified GPU-free (frame shows the dot mid-edge Xử lý→Kết quả, clean light brand). (3) website-to-video = a site-BRANDED showcase workflow (capture site palette/fonts→video) ≠ our fixed-brand SEO explainers → REFERENCE only. Voice-synced reveals + karaoke word-pop (prior turn) also live. | `4_BRAIN/native_composer.py` (`_snap_cues_to_beats` + MotionPath CDN + `.cb-flow`) | SFX on-beat + data-flow motion |
| 2026-07-03 | remotion-dev/remotion + skills — DEEP re-dig (user: "rất hay và rất cần") | (open, MIT) | **RECONSIDERED PROPERLY — user was RIGHT, real value.** Read package SOURCE (spring/interpolate/noise/shapes/media-utils/transitions). Correction to the port-guide's "nothing": that guide meant don't port the CODE; the MATH is learnable. **BUILT: spring physics** → `_spring_ease_path()` ports Remotion's damped-spring solver (ζ<1 overshoot+settle) into a **GSAP `springLand` CustomEase** (CustomEase free since 3.13); hero bignum land now uses real physics, not `back.out`. Path validated (overshoot 1.126, monotonic, ends 1,1) + render-verified (evaluator PASS). Correct architecture note: we emit GSAP tweens (browser interpolates) so Remotion's `interpolate`/`extrapolate` (Python frame-math) does NOT apply — only the ease MATH ports. **Backlog (genuine, not yet built):** `noise2D` seeded simplex → organic blob drift (replace sine); `makePie` → filled pie/wedge charts (we have ring/donut via dashoffset); spring-ease presets (bouncy/smooth/snappy via ζ). SKIP: React components, media-utils FFT wrapper (audiogram niche), layout-utils (we have fitH). | `4_BRAIN/native_composer.py` (`_spring_ease_path` + CustomEase springLand) | physics hero motion |
| 2026-07-03 | Remotion backlog + MotionPath — BUILT 4 (user picked all) | (open craft) | **4 BUILT + render-verified (evaluator PASS):** (1) **spring PRESETS** — `springLand/springSoft/springSnappy/springBouncy` CustomEases (Remotion spring math, ζ tuned per feel) registered in the render doc. (2) **makePie** → NEW `pie` component (Remotion makePie arc-to-centre wedges = SOLID pie, distinct from ring/donut; per-wedge .ritem reveal; categorical palette; reuses donut CSS/validator; in component_picker menu + spec_lint). Frame-verified (62/28/10% wedges, clean light brand). (3) **linechart data-flow** — GSAP MotionPath dot rides the trend line start→end (in-SVG `.lc-flow` circle, guarded). Frame-verified (dot at T4 endpoint). (4) **noise drift** — `window.__nz` seeded value-noise in the doc + `effect_library._blob_noise` makes background blobs MEANDER via noise (seek-safe onUpdate; guarded fallback to sine). All compile clean. | `native_composer.py` (pie, `_spring_ease_path` presets, lc-flow, __nz) + `effect_library.py` (`_blob_noise`) + `component_picker.py` + `spec_lint.py` | physics eases + solid pie + line data-flow + organic blobs |
| 2026-07-03 | Spring presets → auto-directed entrances (user request) | (our engine) | **BUILT + render-verified:** 3 SPRING entrance transitions in `effect_library` — `spring-pop`(springLand), `spring-soft`(springSoft), `spring-snap`(springSnappy) — the component LANDS with real physics. Wired into the content-aware `entrance()` selector: **the director now AUTO-adds the right spring per content** — data(bignum/pie/donut/linechart/stat_grid)→spring-pop, hook/cta/divider→spring-snap, list(steps/checklist/icongrid)→spring-soft, **text(quote/tip)=NO spring** (stays calm). Rotated with the other fitting transitions = tasteful variety, not every scene. Also mapped the new data-viz kinds (pie/donut/linechart/stat_grid/ratio_dots/metric_reveal + checklist/icongrid/layer_stack/org_diagram) into `_KIND_CLASS`. Overridable per-scene via `fx.transition`. Guarded (CustomEase absent → GSAP linear fallback, no crash). Verified: selector picks correct spring per class + forced-spring render PASS. | `4_BRAIN/effect_library.py` (spring transitions + fit pools + KIND_CLASS) | physics entrances, auto-directed |
| 2026-07-03 | D:\SEOSONA AI\Video Template — FRAME-BY-FRAME (user: "phân tích kỹ, rất nhiều tài nguyên quý") | (reference reels) | **56 VN AI/tech 9:16 reels analysed frame-by-frame** (I viewed contact sheets myself + 3 agents covered all 56, ~20 frames each). **Key finding: our component vocabulary is now COMPREHENSIVE** — agents marked ~30 patterns "HAVE"; most "new" suggestions were variants of concept_build/hub/org_diagram/comparison_grid we already have, or off-brand (dark-neon, 3D volumetric, confetti = SKIP). **2 GENUINE gaps BUILT (all 3 agents converged on them) + render-verified:** (1) **`chat`** — messaging conversation UI (user bubble right/accent, AI left/grey, each .ritem builds turn-by-turn) — ubiquitous in AI content. (2) **`codecard`** — code/JSON editor card (filename tab + traffic-lights + `_code_hl` single-pass syntax highlighter: strings green/numbers coral/keywords blue) — DISTINCT from terminal ($ commands). Fixed a highlighter bug (chained regex re-scanned inserted `class=` markup → single-pass tokenizer). Both in component_picker menu+validator + spec_lint. Frame-verified (chat 3-bubble thread + JSON card syntax-lit, clean light brand). **Remaining candidate (flagged, not built): tabbed/segmented list** (Cơ bản/Từ xa/Nâng cao tabs over a list). | `native_composer.py` (chat, codecard, `_code_hl`) + `component_picker.py` + `spec_lint.py` | 2 new components; vocabulary confirmed comprehensive |
| 2026-07-03 | Frame-study consolidation (user: "gom tất cả thành 1 frame-study pipeline dùng được") | (our engine) | **BUILT `tabs` (last frame-study gap) + `4_BRAIN/frame_study.py` = the SINGLE canonical catalog of all 40 scene frames** (group/title/when-cue/schema/guard each), distilled from the 56-reel study + every native_composer component. `component_picker._COMPONENT_MENU` is now GENERATED from `frame_study.menu()` → the LLM picker offers ALL 40 frames (was ~28 hand-listed → the newer pie/chat/codecard/tabs/stat_grid/ratio_dots/layer_stack/ticker_feed/org_diagram etc. were missing → now included = richer, more varied videos). `by_group()` lets the planner rotate across 10 groups (number/chart/compare/list/diagram/dev/media/text/feed/cta); `selfcheck(engine_kinds)` = drift guard (currently ZERO drift). Fallback to the hand-written menu if import fails (never breaks). `tabs` render-verified (Cơ bản/Nâng cao segmented + ✓/◐/🔒 status list, light brand). | `4_BRAIN/frame_study.py` (new) + `native_composer.py` (tabs) + `component_picker.py` (menu from catalog) + `spec_lint.py` | one frame-study kho the pipeline uses; full library offered |
| 2026-07-03 | Video-Template coverage close-out (user: "làm 1-3, không dark mode, dark→light") | (our engine) | **AUDIT + 3 GAP FRAMES + dark→light.** (1) **Audit** = `2_KNOWLEDGE/hyperframes/craft/VIDEO-TEMPLATE-COVERAGE-2026-07.md` — coverage matrix of every LAYOUT pattern across the 56 reels → our frame (0 gaps after this pass); honest 3-tier split (layout=covered / aesthetic=intentionally ONE light style / motion=separate libs). (2) **BUILT the 3 flagged gaps (all LIGHT, render-verified):** `gauge` (radial 240° speedometer — benchmark/speed, distinct from ring; frame-verified 80% arc), `metric_rows` (icon+label+value rows — ⭐Stars 4.7K, distinct from stat_grid), `pill_stack` (vertical option pills — choose-path, distinct from chiprow). (3) **dark→light:** the ONE dark component `terminal` (#0B1220) converted to a LIGHT theme (white card, blue $ prompt, matches codecard) — frame-verified. All 3 added to frame_study.CATALOG (now **43 frames**, selfcheck 0 drift, picker menu auto-includes them) + native_composer + _validate + spec_lint. | `native_composer.py` (gauge/metric_rows/pill_stack + light terminal) + `frame_study.py` + `component_picker.py` + `spec_lint.py` + coverage doc | full 56-reel layout coverage, all light |
| 2026-07-03 | Video-Template TRUE frame-by-frame (user: "phân tích thật kỹ từng frame — element/card/motion/effect/animation/text/bố cục") | (our engine) | **Dense frame-by-frame across ALL 56 reels** (4 agents: ~35-frame sheets + 8-frame MOTION strips each; I ground-truthed 3 strips myself). Analysed 8 DIMENSIONS (component/micro-element/text/item-motion/scene-transition/effect/layout/background) vs our COMPLETE systems. **Critical triage** (agents over-flag — batches 2/3 marked karaoke/chart/donut/spring/shimmer/grain as "GAP" though all in inventory; I code-verified we HAVE ghost-number/blobs/progress-rail/2-tone-underline/dashoffset). **BUILT the 2 genuine gaps (render-verified):** `people` (avatar circle w/ initials-or-photo + name + role — team/contributor; grep confirmed NO avatar existed → new `_avatar()` helper) + `strike_list` (animated cross-out of the "old way" + conclusion chip — the Open Slide before→after retention device; strike line scaleX draws across per item). **Dropped `line-draw`** honestly (would fight our deliberately-DASHED edges + duplicate the existing MotionPath flow dot). **Noted not-built:** corner-tag (marginal micro), flash-cut (off clean-brand), chromatic/glitch/3D-rotate (off-brand). Catalog now **45 frames**, selfcheck 0 drift. Motion vocabulary confirmed comprehensive (karaoke word-pop, spring, data-flow, count-up, reveals, glow — all HAVE). | `native_composer.py` (people, strike_list, `_avatar`, strike motion) + `frame_study.py` + `component_picker.py` + `spec_lint.py` | full 8-dimension coverage; 2 real additions |
