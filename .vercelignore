# Vercel chỉ cần thư mục landing/ để phát hành trang này — phần còn lại của
# repo là mã nguồn dự án, không liên quan tới trang giới thiệu.
#
# Không phải chuyện dọn dẹp cho gọn: OmniClaw nặng 110 MB với gần 15.000 file,
# đủ để build trên Vercel hỏng. Lọc ở đây thì mỗi lần deploy chỉ tải lên vài
# chục KB.
/*
!/landing/
!/vercel.json
