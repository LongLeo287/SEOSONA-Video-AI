# SEOSONA Video — root requirements is a POINTER, not a package list.
# Single source of truth: 0_SETUP/requirements/main.txt (kept in sync with the engine
# consolidation — 2026-07-14: OmniVoice-only voice, PhoWhisper-large-only ASR).
# seosona_doctor / `uv pip install -r requirements.txt` resolve through this include.
-r 0_SETUP/requirements/main.txt
whisperx>=3.1.0  # precise word-align + diarization ASR (asr_router 'whisperx')
