# SEOSONA Video Factory - Dependencies
moviepy>=1.0.3
openai-whisper>=20231117
yt-dlp>=2024.1.1
Pillow>=10.0.0
pyyaml>=6.0
beautifulsoup4>=4.12.0
requests>=2.31.0
python-dotenv>=1.0.0
openai>=1.12.0
google-genai>=1.0.0   # Gemini path in llm_engine (FREE tier: Flash models; replaces the deprecated google-generativeai). Optional — offline NLP fallback runs without a key.
flask>=3.0.0          # 9_DASHBOARD/server.py
playwright>=1.40.0    # scrape screenshots + thumbnail_maker (headless capture)

vieneu>=3.0.0
deep-translator
markitdown>=0.1.6
gitingest
faster-whisper>=1.0.0
ctranslate2>=4.0.0
vietnormalizer>=0.2.3   # VN text-normalization for TTS (dates/%/currency/units→spoken) — native_composer.pron()
sherpa-onnx>=1.12.0  # VN offline ASR engine (asr_router 'sherpa')
underthesea>=6.8.0  # VN text front-end (TTS normalize + subtitle split)
